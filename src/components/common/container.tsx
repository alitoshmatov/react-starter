import { dimensions } from "style/variables";
import styled from "styled-components";

export const Container = styled.div`
  max-width: ${dimensions.containerWidth};
  margin: 0 auto;
  width: 100%;
`;
